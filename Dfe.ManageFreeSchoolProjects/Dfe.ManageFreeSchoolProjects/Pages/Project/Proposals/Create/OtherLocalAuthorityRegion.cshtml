@page "/projects/{projectId}/proposals/create/other-local-authority-region"
@model Dfe.ManageFreeSchoolProjects.Pages.Project.Proposals.Create.OtherLocalAuthorityRegionModel
@using Dfe.ManageFreeSchoolProjects.ViewModels

@{
    var title = "Which region is the other local authority in?";
    ViewData["Title"] = title;
}

@section BeforeMain {
    <partial name="_ProposalBackLink" model="@Model.BackLink" />
}

<partial name="_ProposalRegionSelection"
         model="@(new ProposalRegionSelectionViewModel { Title = title, Region = Model.Region })" />
