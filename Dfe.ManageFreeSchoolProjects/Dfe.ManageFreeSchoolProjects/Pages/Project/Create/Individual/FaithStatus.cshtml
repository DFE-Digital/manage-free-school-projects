@page "/project/create/faith-status"
@using Dfe.ManageFreeSchoolProjects.API.Contracts.Project.Tasks
@model Dfe.ManageFreeSchoolProjects.Pages.Project.Create.Individual.FaithStatusModel

@{
    ViewData["Title"] = "What is the faith status?";

    var faithStatusValues = Enum.GetValues<FaithStatus>().Except(new[] { FaithStatus.NotSet }).ToArray();
    var faithStatuses = faithStatusValues.Select(x => x.ToString()).ToArray();
    var faithStatusHints = faithStatusValues.Select(x => x.ToDescriptionOrEmpty()).ToArray();
}

@section BeforeMain {
    <div role="navigation" class="govuk-extra-width-container">
        <a href="@Model.BackLink" class="govuk-back-link" data-testid="back">Back</a>
    </div>
}

<div class="govuk-form-group">
    <partial name="_ErrorSummary"/>
    <govuk-create-title label="What is the faith status?" add-margin="true" />
    <form method="post">

        <govuk-radios-input name="faith-status"
                            asp-for="@Model.FaithStatus"
                            data-testid="faith-status"
                            labels="@faithStatuses"
                            values="@faithStatuses"
                            hints="@faithStatusHints"/>
        
        <br/>
        <button class="govuk-button" data-module="govuk-button" data-testid="continue">
            Continue
        </button>
    </form>
</div>